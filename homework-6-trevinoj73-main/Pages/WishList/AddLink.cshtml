@page
@model  Final.Pages.WishList.AddLinkModel

@{
    ViewData["Title"] = "Add Link";
}

<h1>Create</h1>

<h4>Link</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div class="form-group">
                <label asp-for="Link.WishId" class="control-label"></label>
                <select asp-for="Link.WishId" asp-items="Model.WishDropDown" class="form-control">
                    <option value="">--Select Card--</option> 
                </select>
                <span asp-validation-for="Link.WishId" class="text-danger"></span>
            </div>
            
            <div class="form-group">
                <input type="submit" value="Add Link" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}